# Terraform
.terraform/
# NOTE: .terraform.lock.hcl is intentionally NOT ignored -- commit it.
# It pins exact provider versions so `terraform init` is reproducible.
*.tfstate
*.tfstate.*
crash.log
crash.*.log
terraform.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
tf*plan*

# OS / editor cruft
.DS_Store
.vscode/
